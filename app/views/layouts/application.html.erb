<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="grad text-light">
    <%= render 'shared/navbar' %>
    <%= render 'shared/notices' %>
    <div class="container">
      <div class="row ">
        <div class="col col-md-1"></div>
        <div class="col col-md-10 container">
            <%= yield %>
        </div>
        <div class="col col-md-1"></div>
      </div>
          
    </div>
    <%= render 'shared/footer' %>
  </body>
</html>
