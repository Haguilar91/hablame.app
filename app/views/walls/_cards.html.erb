
<div class="card p-7 animated fadeInDown  text-light" style=" width: 15rem; background-color:#1E3888; text-align: center;  ">
		<div class="card-body">
		  <h5 class="card-title" >Lic.&nbsp;<%=doctor.name.full%></h5>
		  <%= gravatar_tag @doctor ,:circle, :size => 150, :html => { :class => "img img-fluid card-header" }, :secure => true %>
	  <hr>
		  <h6 class="card-subtitle mb-2 text-muted">Universidad:  <%= doctor.college %></h6>
		  <p class="card-text"><%= doctor.description %></p>

		  <% if current_doctor %>
				<% if doctor.id = current_doctor.id%>
					 <%= link_to "Regresar a Conversación", conversation_path(:id => @conversation.id), class: 'btn btn-danger', :"data-turbolinks" => false %>
				<%else%>
					 <%= link_to "Enviar Mensaje", new_conversation_path(:doctor_id => doctor.id), class: 'btn ', :"data-turbolinks" => false %>
				<%end%>
		  <%else%>
					 <%= link_to "Enviar Mensaje", new_conversation_path(:doctor_id => doctor.id), class: 'container btn text-light ', style: 'background-color: #1B998B;',:"data-turbolinks" => false %>
          <%end%>


		
		  

		  
		</div>
</div>
